# C - File I/O

This project covers file descriptors and the use of the system calls:
`open`, `close`, `read`, and `write`.

## Task 0

- `0-read_textfile.c`: Reads a text file and prints its contents to standard output.
